# comment the line below and uncomment the line above to run the init.sql file locally instead of remotely
# it can't hurt to setup the tables on both in one go, when it comes to ingesting then billing gets involved

